options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: node:18
    entrypoint: bash
    args:
      - -c
      - |
        npm install -g firebase-tools
        firebase deploy --only hosting --project project-a81ea163-b85a-4d88-9d5


